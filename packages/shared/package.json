{
  "name": "@itam/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API contracts, enums and constants for web and api",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
